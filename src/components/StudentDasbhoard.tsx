import { Box, Heading } from "@chakra-ui/react";

const StudentDashboard = () => {
  return (
    <Box p={5}>
      <Heading as="h2">Welcome, Student!</Heading>
      {/* Additional student-specific components */}
    </Box>
  );
};

export default StudentDashboard;
